# معماری پروژه

پلتفرم ثبت محصولات نزدیک به تاریخ انقضا - Monorepo با Nhost (PostgreSQL + Hasura + Auth + Storage + Functions) و Frontend مبتنی بر Vite/React/TypeScript.

جزئیات کامل معماری در فایل اصلی درخواست پروژه ثبت شده و در این سند به‌مرور تکمیل می‌شود.
